package com.david.spring_boot_ecommerce_api.model;

// Enum = conjunto fixo de valores possíveis.
// Aqui estamos definindo os papéis (roles) que um usuário pode ter no sistema.
public enum Role {
    USER,
    ADMIN
}
